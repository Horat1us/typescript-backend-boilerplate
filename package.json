{
  "name": "bobra-scoring",
  "private": true,
  "version": "1.0.0",
  "description": "Bobra Scoring Microservice (back-end)",
  "main": "index.js",
  "scripts": {
    "echo": "echo %pwd%",
    "test": "docker run -v \"%pwd%/tests:/usr/src/app/tests\" bobra-scoring-test",
    "build-test": "docker build --no-cache --rm -t bobra-scoring-test -f Dockerfile.test ."
  },
  "keywords": [
    "bobra",
    "scoring",
    "microservice"
  ],
  "author": "Alexander Letnikow",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^3.3.0",
    "typescript": "^2.6.1"
  },
  "devDependencies": {
    "@types/chai": "^4.0.4",
    "@types/mocha": "^2.2.44",
    "chai": "^4.1.2",
    "mocha": "^4.0.1"
  }
}
